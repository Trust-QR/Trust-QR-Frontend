export const metadata = {
    title: "Signup | Trust QR",
    description: "Here you can Register yourself and you will able register your products",
};

export default function SignupLayout({ children }) {
    return (
        <section>{children}</section>
    )
}