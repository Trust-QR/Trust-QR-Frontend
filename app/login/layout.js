export const metadata = {
    title: "Login | Trust QR",
    description: "Here you can login and you will be redirected to the user dashboard",
};


export default function Layout({ children }) {
    return (
        <section>{children}</section>
    )
}