
export const metadata = {
    title: 'Add Product | Trust QR',
  }


  
export default function RootLayout({ children }) {
    return (
      <section>
        {children}
      </section>
    )
  }
  